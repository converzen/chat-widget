/** @type {import('tailwindcss').Config} */
module.exports = {
  // Every class will look like cvz-flex, cvz-bg-blue-500, etc.
  prefix: 'cvz-', 
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
}
